<%= form_with url: import_staffs_path, method: :post, local: true do |form| %>
  <div class="field">
    <%= form.file_field :csv %>
  </div>

  <div class="actions">
    <%= form.submit 'Upload' %>
  </div>
<% end %>
